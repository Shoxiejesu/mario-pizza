export default class OrderLine{
    piz_id: number;
    quantity: number;
  
  
    constructor(
      piz_id: number,
      quantity: number,
  
    ) {
      this.piz_id = piz_id;
      this.quantity = quantity;
    }
  }
  